[package]
name = "common-base"
version = "0.1.0"
edition = "2021"

[dependencies]
bytes = "1.1"
common-error = { path = "../error" }
memmap2 = "0.5"
paste = "1.0"
serde = { version = "1.0", features = ["derive"] }
snafu = { version = "0.7", features = ["backtraces"] }

[dev-dependencies]
crc = "3.0"
