[package]
name = "table"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1"
serde = "1.0.136"
datatypes = { path = "../datatypes" }
chrono = { version = "0.4", features = ["serde"] }