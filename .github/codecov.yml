# codecov config
coverage:
  status:
    patch: off # disable patch status
    project:
      default:
        enable: yes
        threshold: 1%
ignore:
  - "**/error*.rs" # ignore all error.rs files
  - "common/function-macro" # procedure macro don't run with "panic=abort" which is defined in grcov
